/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findVaultPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const SEND_TO_DISCRIMINATOR = new Uint8Array([
  62, 157, 126, 77, 114, 26, 84, 160,
]);

export function getSendToDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEND_TO_DISCRIMINATOR);
}

export type SendToInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SendToInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type SendToInstructionDataArgs = { amount: number | bigint };

export function getSendToInstructionDataEncoder(): FixedSizeEncoder<SendToInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SEND_TO_DISCRIMINATOR }),
  );
}

export function getSendToInstructionDataDecoder(): FixedSizeDecoder<SendToInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getSendToInstructionDataCodec(): FixedSizeCodec<
  SendToInstructionDataArgs,
  SendToInstructionData
> {
  return combineCodec(
    getSendToInstructionDataEncoder(),
    getSendToInstructionDataDecoder(),
  );
}

export type SendToAsyncInput<
  TAccountSigner extends string = string,
  TAccountVault extends string = string,
  TAccountRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vault?: Address<TAccountVault>;
  recipient: Address<TAccountRecipient>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: SendToInstructionDataArgs["amount"];
};

export async function getSendToInstructionAsync<
  TAccountSigner extends string,
  TAccountVault extends string,
  TAccountRecipient extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SendToAsyncInput<
    TAccountSigner,
    TAccountVault,
    TAccountRecipient,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SendToInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountRecipient,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      signer: expectAddress(accounts.signer.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSendToInstructionDataEncoder().encode(
      args as SendToInstructionDataArgs,
    ),
    programAddress,
  } as SendToInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountRecipient,
    TAccountSystemProgram
  >);
}

export type SendToInput<
  TAccountSigner extends string = string,
  TAccountVault extends string = string,
  TAccountRecipient extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vault: Address<TAccountVault>;
  recipient: Address<TAccountRecipient>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: SendToInstructionDataArgs["amount"];
};

export function getSendToInstruction<
  TAccountSigner extends string,
  TAccountVault extends string,
  TAccountRecipient extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SendToInput<
    TAccountSigner,
    TAccountVault,
    TAccountRecipient,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SendToInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountVault,
  TAccountRecipient,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.recipient),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getSendToInstructionDataEncoder().encode(
      args as SendToInstructionDataArgs,
    ),
    programAddress,
  } as SendToInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountRecipient,
    TAccountSystemProgram
  >);
}

export type ParsedSendToInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    vault: TAccountMetas[1];
    recipient: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: SendToInstructionData;
};

export function parseSendToInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSendToInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      vault: getNextAccount(),
      recipient: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSendToInstructionDataDecoder().decode(instruction.data),
  };
}
